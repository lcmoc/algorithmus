package NumberSorts;

public class insertionSort {
    public insertionSort(int elements[]) {
        int length = elements.length;
        int curr;
        int i,j;

        for (i=0; i < length; i++ ) {
            curr = elements[i];
        }


    }
}
